# BSB container state — entry point into DragonBehaviorSwitchboard.hkb.
#
# Entered via the BSB_DragonEnter global wildcard on BHR_Default.
# Havok lazily loads DragonBehaviorSwitchboard.hkb on first entry;
# BehaviorFileInterceptor serves the runtime-generated bytes.
#
# All registered dragon sub-behaviors (CF cutscene actors, TTTS rider, …)
# are reachable through DSB_SwitchboardSM's wildcard transitions inside
# the loaded switchboard graph.
class: hkbStateMachineStateInfo
name: ST_DragonBSBContainer
stateId: 9
generator: DragonBSBContainerBFR
